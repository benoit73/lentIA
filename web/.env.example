# Pour `npm run dev` en local : copier en `.env.local` et renseigner le
# Client ID OAuth Google (voir README.md racine du projet, section OAuth).
VITE_GOOGLE_CLIENT_ID=xxxxxxxxxx.apps.googleusercontent.com
